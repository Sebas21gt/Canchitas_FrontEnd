import 'package:flutter/material.dart';

class ChampionshipFormScreen extends StatelessWidget {
  const ChampionshipFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Championship Form Screen"),);
  }
}