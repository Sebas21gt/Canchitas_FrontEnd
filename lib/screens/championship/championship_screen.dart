import 'package:flutter/material.dart';

class ChampionshipScreen extends StatelessWidget {
  const ChampionshipScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Championship Screen"),
    );
  }
}